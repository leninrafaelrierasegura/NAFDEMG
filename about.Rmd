---
title: "Contents"
date: "Last modified: `r format(Sys.time(), '%d-%m-%Y.')`"
output:
  html_document:
    mathjax: "https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"
    highlight: pygments
    theme: flatly
    code_folding: hide # class.source = "fold-hide" to hide code and add a button to show it
    df_print: paged
    toc: true
    toc_float:
      collapsed: true
      smooth_scroll: true
    number_sections: true
    fig_caption: true
    code_download: true
    css: visual.css
always_allow_html: true
bibliography: 
  - references.bib
  - grateful-refs.bib
header-includes:
  - \newcommand{\ar}{\mathbb{R}}
  - \newcommand{\llav}[1]{\left\{#1\right\}}
  - \newcommand{\pare}[1]{\left(#1\right)}
  - \newcommand{\Ncal}{\mathcal{N}}
  - \newcommand{\Vcal}{\mathcal{V}}
  - \newcommand{\Ecal}{\mathcal{E}}
  - \newcommand{\Wcal}{\mathcal{W}}
---

```{r, echo = FALSE}
# Start the figure counter
fig_count <- 0
# Define the captioner function
captioner <- function(caption) {
  fig_count <<- fig_count + 1
  paste0("Figure ", fig_count, ": ", caption)
}
```


```{css, echo = FALSE}
.custom-box {
  background-color: #f5f7fa; /* Light grey-blue background */
  border-color: #e1e8ed; /* Light border color */
  color: #2c3e50; /* Dark text color */
  padding: 15px; /* Padding inside the box */
  border-radius: 5px; /* Rounded corners */
  margin-bottom: 20px; /* Spacing below the box */
}
```

This page shows a brief description of the contents of this website.

::: {.custom-box}
# Contents
:::

__________

  - [Contents](about.html): The current page.
  
__________

::: {.custom-box}
# Preliminaries
:::

__________

  - [Preliminaries](preliminaries.html): This page shows 
    - [how the finite element basis functions are constructed on a metric graph](preliminaries.html#fem-basis) (see the [Basis](basis.html#basisf) page for an additional illustration),
    - [how the eigenvalues and eigenfunctions of the shifted Kirchhoff–Laplacian $(\kappa^2-\Delta_\Gamma)$ operator are computed on the tadpole graph,](preliminaries.html#eigenfunctions)
    - [and how to project onto a fine space-time mesh.](preliminaries.html#piecewise_projection)
     

__________

::: {.custom-box}
# Results for the *Numerical approximation of fractional diffusion equations on metric graphs* paper
:::

__________

The illustration below was produced [here](preliminaries.html#fem-basis).

```{r, echo = FALSE, fig.height = 6, out.width = "100%", fig.cap = captioner("Illustration of the system of basis functions $\\{\\varphi_j^e, \\phi_v\\}$ on the tadpole graph. Notice that the sets $\\mathcal{N}_{v}$ are depicted in green and their corresponding basis functions are shown in red.")}
load(here::here("data_files/fem_basis_tadpole.RData"))
p_b1
```

__________

  - [Functionality](functionality.html): This page shows 
    - [the derivation of the proposed numerical scheme,](functionality.html#num_scheme)
    - [the implementation of the numerical scheme,](functionality.html#num_implementation)
    - [the implementation of some auxiliary functions,](functionality.html#auxiliary_functions)
    - [and the implementation of some plotting functions.](functionality.html#plotting_functions)
    
__________

  - [Experiment](exp1.html): This page shows 
    - [how to construct an exact solution to the addressed problem for error analysis purposes.](exp1.html#eigensolconst)
    
    

```{r, echo = FALSE, fig.height = 6, out.width = "100%", fig.cap = captioner("Time evolution of the right-hand side function $f$.")}
load(file = here::here("data_files/exp1_fig.RData"))
exp1_fig
```


```{r, echo = FALSE, fig.height = 6, out.width = "100%", fig.cap = captioner("Time evolution of the absolute difference between the exact and approximate solution.")}
load(file = here::here("data_files/exp1_error.RData"))
exp1_error
```
__________

  - [Convergence in 𝘩](conv_in_h.html): This page shows 
    - [the convergence with respect to the spatial mesh size 𝘩.](conv_in_h.html)


  - [Convergence in τ](conv_in_tau.html): This page shows 
    - [the convergence with respect to the temporal mesh size τ.](conv_in_tau.html)


  - [Convergence in 𝑚](conv_in_m.html): This page shows 
    - [the convergence with respect to the order 𝑚 of the rational approximation.](conv_in_m.html)
    

  Figure 1 shows the convergence results. For details on how these plots were generated, please refer to the hyperlinks above.

**Metric graph**
      
```{r, eval = FALSE, echo = FALSE}
library(patchwork)
library(ggplot2)
load(here::here("data_files/p_h_tadpole.RData"))
load(here::here("data_files/p_tau_tadpole.RData"))
load(here::here("data_files/p_m_tadpole.RData"))

load(here::here("data_files/p_h_nicer_tadpole.RData"))
load(here::here("data_files/p_tau_nicer_tadpole.RData"))
load(here::here("data_files/p_m_nicer_tadpole.RData"))

p_all_tadpole <- (p_h_tadpole | p_tau_tadpole | p_m_tadpole) + 
  plot_annotation(
    title = expression("         Convergence behavior on the tadpole graph"),
    theme = theme(plot.title = element_text(size = 18, face = "bold", hjust = 0.5, 
        family = "Palatino"))
  )


p_all_nicer_tadpole <- (p_h_nicer_tadpole | p_tau_nicer_tadpole | p_m_nicer_tadpole) +
  plot_annotation(
    title = expression("         Convergence behavior on the tadpole graph"),
    theme = theme(plot.title = element_text(size = 18, face = "plain", hjust = 0.5,
        family = "Palatino"))
  )

myggsave(p_all_nicer_tadpole, width = 12, height = 6, use_mathpazo = TRUE)
ggplot2::ggsave(here::here("data_files/p_all_tadpole.png"), width = 12, height = 6, plot = p_all_tadpole, dpi = 300)
```


```{r, echo = FALSE, fig.align='center', out.width = "1200px", out.height = "600px", fig.cap = captioner("Comparison of theoretical and observed convergence behavior for the $L_2(\\Gamma\\times(0,T))$-error with respect to $h$, $\\tau$, and $m$. The left and center plots display the convergence rates in $h$ and $\\tau$, respectively, on a $\\text{log}_{10}$–$\\text{log}_{10}$ scale, while the right plot shows the exponential decay in $m$ on a semi-$\\text{log}_{e}$ scale, with $m$ plotted on a square-root scale. Dashed lines indicate the theoretical rates, and solid lines represent the observed error curves. The legend below each plot shows the value of $\\alpha$ along with the corresponding theoretical ('theo'), and observed ('obs') rates for each case.")}
knitr::include_graphics(here::here("data_files/tikzpic/p_all_nicer_tadpole_with_mathpazo.pdf"))
```


**Metric graph - with logtau**

```{r, eval = FALSE, echo = FALSE}
library(patchwork)
library(ggplot2)
load(here::here("data_files/p_h_tadpole_with_logtau.RData"))
load(here::here("data_files/p_tau_tadpole_with_logtau.RData"))
load(here::here("data_files/p_m_tadpole_with_logtau.RData"))

load(here::here("data_files/p_h_nicer_tadpole_with_logtau.RData"))
load(here::here("data_files/p_tau_nicer_tadpole_with_logtau.RData"))
load(here::here("data_files/p_m_nicer_tadpole_with_logtau.RData"))

p_all_tadpole_with_logtau <- (p_h_tadpole_with_logtau | p_tau_tadpole_with_logtau | p_m_tadpole_with_logtau) + 
  plot_annotation(
    title = expression("         Convergence behavior on the tadpole graph"),
    theme = theme(plot.title = element_text(size = 18, face = "bold", hjust = 0.5, 
        family = "Palatino"))
  )


p_all_nicer_tadpole_with_logtau <- (p_h_nicer_tadpole_with_logtau | p_tau_nicer_tadpole_with_logtau | p_m_nicer_tadpole_with_logtau)
# +
#   plot_annotation(
#     title = expression("         Convergence behavior on the tadpole graph"),
#     theme = theme(plot.title = element_text(size = 18, face = "plain", hjust = 0.5,
#         family = "Palatino"))
#   )

myggsave(p_all_nicer_tadpole_with_logtau, width = 12, height = 5, use_mathpazo = FALSE)
ggplot2::ggsave(here::here("data_files/p_all_tadpole_with_logtau.png"), width = 12, height = 5, plot = p_all_tadpole_with_logtau, dpi = 300)
```


```{r, echo = FALSE, fig.align='center', out.width = "1200px", out.height = "600px", fig.cap = captioner("Comparison of theoretical and observed convergence behavior for the $L_2(\\Gamma\\times(0,T))$-error with respect to $h$, $\\tau$, and $m$. The left and center plots display the convergence rates in $h$ and $\\tau$, respectively, on a $\\text{log}_{10}$–$\\text{log}_{10}$ scale, while the right plot shows the exponential decay in $m$ on a semi-$\\text{log}_{e}$ scale, with $m$ plotted on a square-root scale. Dashed lines indicate the theoretical rates, and solid lines represent the observed error curves. The legend below each plot shows the value of $\\alpha$ along with the corresponding theoretical ('theo'), and observed ('obs') rates for each case.")}
knitr::include_graphics(here::here("data_files/tikzpic/p_all_nicer_tadpole_with_logtau_with_mathpazo.pdf"))
```



**Rectangle**

```{r, eval = FALSE, echo = FALSE}
library(patchwork)
library(ggplot2)
load(here::here("data_files/p_h_square.RData"))
load(here::here("data_files/p_tau_square.RData"))
load(here::here("data_files/p_m_square.RData"))
load(here::here("data_files/p_h_nicer_square.RData"))
load(here::here("data_files/p_tau_nicer_square.RData"))
load(here::here("data_files/p_m_nicer_square.RData"))

p_all_square <- (p_h_square | p_tau_square | p_m_square) + 
  plot_annotation(
    title = expression("         Convergence behavior on the unit square"),
    theme = theme(plot.title = element_text(size = 18, face = "bold", hjust = 0.5, 
        family = "Palatino"))
  )


p_all_nicer_square <- (p_h_nicer_square | p_tau_nicer_square | p_m_nicer_square) +
  plot_annotation(
    title = expression("         Convergence behavior on the unit square"),
    theme = theme(plot.title = element_text(size = 18, face = "plain", hjust = 0.5,
        family = "Palatino"))
  )

myggsave(p_all_nicer_square, width = 12, height = 6, use_mathpazo = TRUE)
ggplot2::ggsave(here::here("data_files/p_all_square.png"), width = 12, height = 6, plot = p_all_square, dpi = 300)
```


```{r, echo = FALSE, fig.align='center', out.width = "1200px", out.height = "600px", fig.cap = captioner("Comparison of theoretical and observed convergence behavior for the $L_2(\\Gamma\\times(0,T))$-error with respect to $h$, $\\tau$, and $m$. The left and center plots display the convergence rates in $h$ and $\\tau$, respectively, on a $\\text{log}_{10}$–$\\text{log}_{10}$ scale, while the right plot shows the exponential decay in $m$ on a semi-$\\text{log}_{e}$ scale, with $m$ plotted on a square-root scale. Dashed lines indicate the theoretical rates, and solid lines represent the observed error curves. The legend below each plot shows the value of $\\alpha$ along with the corresponding theoretical ('theo'), and observed ('obs') rates for each case.")}
knitr::include_graphics(here::here("data_files/tikzpic/p_all_nicer_square_with_mathpazo.pdf"))
```

**Sphere**

```{r, eval = FALSE, echo = FALSE}
library(patchwork)
library(ggplot2)
load(here::here("data_files/p_h_sphere.RData"))
load(here::here("data_files/p_tau_sphere.RData"))
load(here::here("data_files/p_m_sphere.RData"))
load(here::here("data_files/p_h_nicer_sphere.RData"))
load(here::here("data_files/p_tau_nicer_sphere.RData"))
load(here::here("data_files/p_m_nicer_sphere.RData"))

p_all_sphere <- (p_h_sphere | p_tau_sphere | p_m_sphere) + 
  plot_annotation(
    title = expression("         Convergence behavior on the unit sphere"),
    theme = theme(plot.title = element_text(size = 18, face = "bold", hjust = 0.5, 
        family = "Palatino"))
  )


p_all_nicer_sphere <- (p_h_nicer_sphere | p_tau_nicer_sphere | p_m_nicer_sphere) +
  plot_annotation(
    title = expression("         Convergence behavior on the unit sphere"),
    theme = theme(plot.title = element_text(size = 18, face = "plain", hjust = 0.5,
        family = "Palatino"))
  )

myggsave(p_all_nicer_sphere, width = 12, height = 6, use_mathpazo = TRUE)
ggplot2::ggsave(here::here("data_files/p_all_sphere.png"), width = 12, height = 6, plot = p_all_sphere, dpi = 300)
```


```{r, echo = FALSE, fig.align='center', out.width = "1200px", out.height = "600px", fig.cap = captioner("Comparison of theoretical and observed convergence behavior for the $L_2(\\Gamma\\times(0,T))$-error with respect to $h$, $\\tau$, and $m$. The left and center plots display the convergence rates in $h$ and $\\tau$, respectively, on a $\\text{log}_{10}$–$\\text{log}_{10}$ scale, while the right plot shows the exponential decay in $m$ on a semi-$\\text{log}_{e}$ scale, with $m$ plotted on a square-root scale. Dashed lines indicate the theoretical rates, and solid lines represent the observed error curves. The legend below each plot shows the value of $\\alpha$ along with the corresponding theoretical ('theo'), and observed ('obs') rates for each case.")}
knitr::include_graphics(here::here("data_files/tikzpic/p_all_nicer_sphere_with_mathpazo.pdf"))
```
__________
  
::: {.custom-box}    
# Results for the *Optimal control of fractional diffusion equations on metric graphs* paper
:::
__________


```{r, echo = FALSE, fig.height = 8, out.width = "100%", fig.cap = captioner("Illustration of the basis function system $\\{\\psi^i_h\\}_{i=1}^{N_h}$ on the hat-basis-functions graph (in black). Standard hat functions associated with internal edge nodes are shown in blue, while special vertex-centered functions are highlighted in red.")}
load(file = here::here("data_files/p_basis.RData"))
p_basis
```


  - [Control Functionality](control_functionality.html): This page shows 
    - [the description of the optimal control problem,](control_functionality.html#optimal_control_problem)
    - [how to implement a numerical scheme to numerically solve the optimal control problem,](control_functionality.html#num_scheme_optim_control)
    - [and some details on the convergence analysis for the FBSM.](control_functionality.html#conv_FBSM)

__________

  - [Optimal Control](control2.html): This page shows 
    - [how to construct an exact solution for the optimal control problem for error analysis purposes.](control2.html#exact_sol_for_optim_control_prob)

__________

  - [Convergence in 𝘩](control_conv_h_with_iteration.html): This page shows 
    - [the convergence with respect to the spatial mesh size 𝘩.](control_conv_h_with_iteration.html)
 

```{r, echo = FALSE, fig.align='center', fig.dim= c(12,6), fig.cap = captioner("Comparison of theoretical and observed convergence behavior for the $L_2(\\Gamma\\times(0,T))$-error with respect to $h$ on a $\\text{log}_{10}$–$\\text{log}_{10}$ scale. Dashed lines indicate the theoretical rates, and solid lines represent the observed error curves. The legend below each plot shows the value of $\\alpha$ along with the corresponding theoretical ('theo'), and observed ('obs') rates for each case.")}
knitr::include_graphics(here::here("data_files/control_conv_rates_h_all_with_iteration.png"))
```
     
__________

  - [Convergence in τ](control_conv_tau_with_iteration.html): This page shows 
    - [the convergence with respect to the temporal mesh size τ.](control_conv_tau_with_iteration.html)


```{r, echo = FALSE, fig.align='center', fig.dim= c(12,6), fig.cap = captioner("Comparison of theoretical and observed convergence behavior for the $L_2(\\Gamma\\times(0,T))$-error with respect to $\\tau$ on a $\\text{log}_{10}$–$\\text{log}_{10}$ scale. Dashed lines indicate the theoretical rates, and solid lines represent the observed error curves. The legend below each plot shows the value of $\\alpha$ along with the corresponding theoretical ('theo'), and observed ('obs') rates for each case.")}
knitr::include_graphics(here::here("data_files/control_conv_rates_tau_all_with_iteration.png"))
```
         
__________

  - [Convergence in 𝑚](control_conv_m_with_iteration.html): This page shows 
    - [the convergence with respect to the order 𝑚 of the rational approximation.](control_conv_m_with_iteration.html)

      
```{r, echo = FALSE, fig.align='center', fig.dim= c(12,6), fig.cap = captioner("Comparison of theoretical and observed convergence behavior for the $L_2(\\Gamma\\times(0,T))$-error with respect to $m$ on a semi-$\\text{log}_{e}$ scale, with $m$ plotted on a square-root scale. Dashed lines indicate the theoretical rates, and solid lines represent the observed error curves. The legend below each plot shows the value of $\\alpha$ along with the corresponding theoretical ('theo'), and observed ('obs') rates for each case.")}
knitr::include_graphics(here::here("data_files/control_conv_rates_m_all_with_iteration.png"))
```    
      
__________ 


 - The following figures show the convergence results for the optimal state and control variables. They contain the same information as the above plots, but are combined into two single figures for each variable.



```{r, eval = FALSE, echo = FALSE}
library(patchwork)
library(ggplot2)
load(here::here("data_files/p_h_u.RData"))
load(here::here("data_files/p_tau_u.RData"))
load(here::here("data_files/p_m_u.RData"))
load(here::here("data_files/p_h_u_nicer_tadpole.RData"))
load(here::here("data_files/p_tau_u_nicer_tadpole.RData"))
load(here::here("data_files/p_m_u_nicer_tadpole.RData"))
p_all_u <- (p_h_u | p_tau_u | p_m_u) + 
  plot_annotation(
    title = expression("         Convergence behavior for the optimal state variable " * italic(bar(u))),
    theme = theme(plot.title = element_text(size = 18, face = "bold", hjust = 0.5, 
        family = "Palatino"))
  )
p_all_u_nicer_tadpole <- (p_h_u_nicer_tadpole | p_tau_u_nicer_tadpole | p_m_u_nicer_tadpole) + 
  plot_annotation(
    title = "$\\mbox{Convergence behavior for the optimal state variable }\\bar{u}$",
    theme = theme(plot.title = element_text(size = 18, face = "plain", hjust = 0.5, 
        family = "Palatino"))
  )

myggsave(p_all_u_nicer_tadpole, width = 12, height = 6, use_mathpazo = TRUE)
ggplot2::ggsave(here::here("data_files/p_all_u.png"), width = 12, height = 6, plot = p_all_u, dpi = 300)
```


```{r, echo = FALSE, fig.align='center', out.width = "1200px", out.height = "600px", fig.cap = captioner("Comparison between theoretical and observed convergence behavior for the error $\\|\\bar{u}-\\bar{U}_{h,m}^\\tau\\|_{L_2(\\Gamma\\times(0,T))}$ with respect to $h$, $\\tau$, and $m$. The left and center plots display the convergence rates in $h$ and $\\tau$, respectively, on a $\\text{log}_{10}$–$\\text{log}_{10}$ scale, while the right plot shows the exponential decay in $m$ on a semi-$\\text{log}_{e}$ scale, with $m$ plotted on a square-root scale. Dashed lines indicate the theoretical rates, and solid lines represent the observed error curves. The legend below each plot shows the value of $\\alpha$ along with the corresponding theoretical ('theo'), and observed ('obs') rates for each case.")}
knitr::include_graphics(here::here("data_files/tikzpic/p_all_u_nicer_tadpole_with_mathpazo.pdf"))
```

__________ 


```{r, eval = FALSE, echo = FALSE}
library(patchwork)
library(ggplot2)
load(here::here("data_files/p_h_z.RData"))
load(here::here("data_files/p_tau_z.RData"))
load(here::here("data_files/p_m_z.RData"))
load(here::here("data_files/p_h_z_nicer_tadpole.RData"))
load(here::here("data_files/p_tau_z_nicer_tadpole.RData"))
load(here::here("data_files/p_m_z_nicer_tadpole.RData"))
p_all_z <- (p_h_z | p_tau_z | p_m_z) + 
  plot_annotation(
    title = expression("         Convergence behavior for the optimal control variable " * italic(bar(z))),
    theme = theme(plot.title = element_text(size = 18, face = "bold", hjust = 0.5, 
        family = "Palatino"))
  )
p_all_z_nicer_tadpole <- (p_h_z_nicer_tadpole | p_tau_z_nicer_tadpole | p_m_z_nicer_tadpole) + 
  plot_annotation(
    title = "$\\mbox{Convergence behavior for the optimal control variable }\\bar{z}$",
    theme = theme(plot.title = element_text(size = 18, face = "plain", hjust = 0.5, 
        family = "Palatino"))
  )

myggsave(p_all_z_nicer_tadpole, width = 12, height = 6, use_mathpazo = TRUE)
ggplot2::ggsave(here::here("data_files/p_all_z.png"), width = 12, height = 6, plot = p_all_z, dpi = 300)
```


```{r, echo = FALSE, fig.align='center', out.width = "1200px", out.height = "600px", fig.cap = captioner("Comparison between theoretical and observed convergence behavior for the error $\\|\\bar{z}- \\bar{\\mathcal{Z}}_{h,m}^\\tau\\|_{L_2(\\Gamma\\times(0,T))}$ with respect to $h$, $\\tau$, and $m$. The left and center plots display the convergence rates in $h$ and $\\tau$, respectively, on a $\\text{log}_{10}$–$\\text{log}_{10}$ scale, while the right plot shows the exponential decay in $m$ on a semi-$\\text{log}_{e}$ scale, with $m$ plotted on a square-root scale. Dashed lines indicate the theoretical rates, and solid lines represent the observed error curves. The legend below each plot shows the value of $\\alpha$ along with the corresponding theoretical ('theo'), and observed ('obs') rates for each case.")}
knitr::include_graphics(here::here("data_files/tikzpic/p_all_z_nicer_tadpole_with_mathpazo.pdf"))
```
    

__________ 

 
  - The following illustration was produced [here](control_functionality.html#contraction_constant_comparison).

      
```{r, echo = FALSE, fig.align='center', out.width = "1200px", out.height = "700px", fig.cap = captioner("Comparison of the contraction constant $\\gamma =  \\tau^2\\|\\mathbf{T}\\|_2/\\mu$ and its theoretical upper bound $1 / (\\mu \\kappa^{4 \\beta})$ as functions of the sample size $N$ for $T = 2$. Different colors, shapes, and line types correspond to variations in $\\kappa$, $\\beta$, and $\\mu$, respectively. Both plots have their x- and y-axes on a $\\log_{10}$ scale, and they share the same y-axis limits for direct comparability.")}
knitr::include_graphics(here::here("data_files/tikzpic/fixedpointconvergence_combined2_with_mathpazo.pdf"))
```
      
__________
  
::: {.custom-box}    
# Poster
:::

 - [Poster](PosterLeninStatWorkshop2025.html): This page shows the poster presented at [The KAUST 2025 Workshop on Statistics](https://cemse.kaust.edu.sa/events/by-type/workshop/2025/11/02/kaust-2025-workshop-statistics).
 
__________

::: {.custom-box}    
# Presentation
:::

- [Presentation](Presentation.html): This page shows a presentation about the two papers.

__________

      
      